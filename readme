a Hash table ADT that supports the following actions:

HashCreate - creates a new generic Hash Table.

HashAdd - inserts a new element to the Hash Table.

HashFind - finds an element according to a given key.

HashRemove - removes an element according to a given key.

HashPrint - prints the Hash Table.

HashDestroy - destroys the  Hash Table.


there is also a dictionary implementation using the generic Hash Table.
